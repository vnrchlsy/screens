# screens

---

**Icon-cruft hook (opt-in):** Google Drive's Mirror sync periodically re-creates zero-byte `Icon\r` files inside `.git/`. Enable the shared purge hook with `git config core.hooksPath .githooks`. See `../dev/HANDOFF.md`.
